# Godot 4+ specific ignores
.godot/
.nomedia

# Godot-specific ignores
.import/
export.cfg
export_credentials.cfg
*.tmp

# Imported translations (automatically generated from CSV files)
*.translation

# Mono-specific ignores
.mono/
data_*/
mono_crash.*.json

# Harness local config (personal overrides, not shared)
.claude/settings.local.json

# Scheduled tasks (session-specific, not shared)
.claude/scheduled_tasks.json

# Session transcripts (session data, not project code)
.claude/transcripts/

# State manifest (session state, not project code)
.claude/state.json

# Agent worktrees (transient, session-specific)
.claude/worktrees/

# GdUnit4 test adapter (auto-generated)
gdunit4_testadapter/
